# Copilot instructions (bindery)

Read `AGENTS.md` first. It is the canonical Bindery Core guidance — the two
subsystems and their boundaries, invariants, validation targets, working method,
logging fields, protobuf and CRD rules, and conventions.

There are no Copilot-specific overrides. Record new guidance in `AGENTS.md`, not
here.
