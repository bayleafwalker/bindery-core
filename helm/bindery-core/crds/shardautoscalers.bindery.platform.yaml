---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: shardautoscalers.bindery.platform
spec:
  group: bindery.platform
  names:
    kind: ShardAutoscaler
    listKind: ShardAutoscalerList
    plural: shardautoscalers
    shortNames:
    - sa
    singular: shardautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.worldRef.name
      name: World
      type: string
    - jsonPath: .spec.minShards
      name: Min
      type: integer
    - jsonPath: .spec.maxShards
      name: Max
      type: integer
    - jsonPath: .status.currentShards
      name: Current
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ShardAutoscaler automatically scales the number of shards for
          a WorldInstance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              maxShards:
                description: MaxShards is the maximum number of shards.
                format: int32
                minimum: 1
                type: integer
              metrics:
                description: |-
                  Metrics defines the metrics to use for scaling.
                  Currently only supports CPU and Memory utilization.
                items:
                  properties:
                    resource:
                      description: Resource defines the resource metric.
                      properties:
                        name:
                          description: Name is the name of the resource (cpu, memory).
                          type: string
                        targetAverageUtilization:
                          description: |-
                            TargetAverageUtilization is the target value of the average of the
                            resource metric across all relevant pods, represented as a percentage of
                            the requested value of the resource for the pods.
                          format: int32
                          type: integer
                      required:
                      - name
                      type: object
                    type:
                      description: Type is the type of metric (e.g., "Resource").
                      type: string
                  required:
                  - type
                  type: object
                type: array
              minShards:
                description: MinShards is the minimum number of shards.
                format: int32
                minimum: 1
                type: integer
              worldRef:
                description: WorldRef references the WorldInstance to scale.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
            required:
            - maxShards
            - minShards
            - worldRef
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentShards:
                format: int32
                type: integer
              desiredShards:
                format: int32
                type: integer
              lastScaleTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            required:
            - currentShards
            - desiredShards
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
