{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bindery-external-runtime.serviceAccountName" . }}
  labels:
    {{- include "bindery-external-runtime.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}

